## 🥶 InvalidDefinitionException - No serializer found for class

외부 API를 연동해서 응답을 가져오던 중 직렬화를 할 수 없다는 오류가 나타났다.  
직렬화라는 건 object를 JSON과 같은 형식으로 변경해서 DTO에 데이터를 담을 때 형식이 변경되는 것으로 알고 있었다. (반대로는 역직렬화)

---

## 👀 상황

JSON 응답을 처리하기 위한 DTO 클래스를 만들고 그 안에 중첩 클래스를 만들어두었다.  
`MovieListResponse -> MovieListResult -> Movie`

스프링은 직렬화를 할 때 라이브러리인 Jackson을 사용하는데, 얘는 private인 필드에 직접 접근할 수 없다.

기본적으로는 기본 생성자가 존재해야 하며, getter/setter를 만들어주거나, nested를 풀어서 각각의 개별 클래스로 만들거나,  
아니면 `@JsonProperty` 혹은 `@JsonAutoDetect`(많은 JSON 애노테이션이 있긴 하지만...)을 붙여주는 것이 방법이라고 한다.

일단 접근제어자를 public으로 푸는 것이나 getter/setter를 만드는 것이  
객체지향적 관점에서 캡슐화를 약화시킨다고 책에서 읽었는데, 서치할 때에는 모든 글의 옵션에 들어가 있었다.  
이유는 getter/setter는 DTO와 같이 데이터를 나르는 단순 컨테이너 같은 예외적인 클래스에는  
객체지향을 엄격히 적용하지 않아도 괜찮다는 것이었다.  

→ 단순히 객체지향적 설계는 getter/setter를 지양하는 것이 아니라는 것을 알게 되었고,  
내가 만들어 사용하는 클래스들의 역할을 좀 더 깊게 생각하며 써야겠다고 생각했다.


## 🧯 해결

어찌 되었든 애노테이션을 붙여서 디버깅을 해보는 게 가장 빠른 방법이기도 해서  
`@JsonAutoDetect(fieldVisibility = JsonAutoDetect.Visibility.ANY)`를 클래스마다 붙여주었다.  
결과는 성공적이었다. 그런데 이건 뭔데 성공할까?

일단 `@JsonAutoDetect`는 모든 필드를 직렬화/역직렬화 되도록 한다.  
이는 직렬화할 필드들만 getter를 만들어주는 것과는 다르게,  
이게 private이든 public이든, getter가 있든 없든, 외계인이든(?) 모두 직렬화 또는 역직렬화 대상으로 포함시키기 때문이다.

이렇게 간단한 방법이면 뭔가 단점이 존재할 것 같았다.  
찾아보니 단점이 있긴 했다.

1. 이 애노테이션을 모르는 사람은 유지보수할 때 찾아봐야 해서 시간 낭비가 될 수 있다.  
2. 다른 라이브러리나 테스트에서 예측이 어려움  
3. getter/setter가 없어서 이 또한 유지보수할 때 헷갈릴 가능성이 있음  

하지만 이게 작은 사이드 프로젝트여서 여러 가지를 사용해보고 싶기도 하고,  
백엔드는 나만 유지보수할 가능성이 크기도 하고,  
가장 큰 이유로는 데이터를 받아오기만 하는 단순 DTO 용도의 클래스이기 때문에  
뭔가 클래스를 크게 변경할 이유가 없다고 생각했어서 사용하려고 한다!

다음엔 JSON에 대해서 더 공부해야겠다.
